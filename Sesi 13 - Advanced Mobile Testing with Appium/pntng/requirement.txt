You have 
- visual studio
- Android studio
- SDK
set emulator in android studio

lets go
1.create folder 
2.create package.json use syntax = npm init, if it is already ,install npm init wdio.
3.cmd pops up a question
- Testing type : E2E Testing - of Web or Mobile Application
- Automation backend located : On my local machine
- Environment : Mobile
- Mobile Environment : Android
- Framework : Mocha
- Typescript : Yes
- Plugin : wait-for
- Add Service : appium

on Appium CLI option :
- Install appium server.
- Install appium driver : uiautomator2.
- Run appium doctor.
4. Create folder use name apk, next file app move to apk
5. On file wdio.conf.ts add syntax command & put on top :
- Import * as path from ‘pathʼ
- const appPath = path.join(__dirname, 'apk', '{name_apk}.apk');
example in folder to .txt check 1-3
6. set up capabilities 60-68
- in device change name is the emulator earlier,  match name with emulator
7. setup mobile device change its 117-125
8.create folder test ot tests
- pageobjects
- actions
- specs
9.fill the folder with files like the example
10.install report use allure : npm install --save-dev @wdio/allure-reporter allure-commandline
11. for run
 1-  appiu
 2-  npx wdio run .\wdio.conf.ts     
 3-  npx allure serve allure-results      